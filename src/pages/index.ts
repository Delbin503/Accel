// Pages are imported directly by the router — not re-exported here —
// so React Router's lazy() can code-split each route independently.
// Example usage in routes:
//   const Dashboard = React.lazy(() => import("@/pages/dashboard"));
